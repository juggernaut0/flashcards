app {
  port = 9002
}

auth {
  host = "localhost"
  port = 9001
}

data {
  user = "flashcards"
  password = "flashcards"
  jdbcUrl = "jdbc:postgresql://localhost:6432/flashcards"
  r2dbcUrl = "r2dbc:pool:postgresql://localhost:6432/flashcards"
}
